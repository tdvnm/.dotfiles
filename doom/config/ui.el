;;; config/ui.el -*- lexical-binding: t; -*-

(setq doom-font (font-spec :family "Monaspace Neon NF" :size 14)
      doom-variable-pitch-font (font-spec :family "Monaspace Neon NF" :size 14))

(setq doom-theme 'base16-horizon-sakura)

(setq display-line-numbers-type 'relative)
